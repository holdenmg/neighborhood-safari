<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <a href="/profile/chooseAnimal">Create a New Post </a>



    {{#if posts.length}}
    <div class="col-md-6 post-list">
      <h3>Your Posts:</h3>

      {{#each posts as |post|}}
      <div class="card">
  <header id="logo-blue" class="card-header">
    <p class="card-header-title">
      {{animal.common_name}}
    </p>
   
    
    <button class="card-header-icon" aria-label="more options">
      <span class="icon">
        <i class="fas fa-angle-down" aria-hidden="true"></i>
      </span>
    </button>
  </header>
  <div id="logo-grey" class="card-content">
    <div class="content">
      <h6>{{post.city}} {{post.state}}</h6>
   <h6>{{post.address}}</h6>
   {{#if post.danger}}
   <h6 class="has-text-danger">DANGER</h6>
   {{/if}}
   {{#if post.missing}}
   <h6 class="has-text-success"> MISSING </h6>
   {{/if}}
    <p>Created on {{format_date post.date_created}}</p>
    </div>
    <footer class="card-footer">
      <div class="field is-grouped">
        
    <a href="/post/{{post.id}}" class="card-footer-item">
    <button id="logo-blue" class="button is-info">See Post</button>
    </a>
   
    
          <a href="/profile/edit/{{post.id}}" class="card-footer-item">
          <button id="logo-green" class="button is-success">
            UPDATE
            </button>
          </a>
        
          <a class="card-footer-item">
         
          <button class="button is-danger"  data-id="{{post.id}}">DELETE</button>
          </a>
    </div>
  </footer>
  </div>
  
       
      </div>
      {{/each}}
    </div>
    {{/if}}
  </div>

  <script src="./js/profile.js"></script>